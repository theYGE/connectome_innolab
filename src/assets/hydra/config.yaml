params:
  epochs: 100
  out_channels_vgae: 5
  out_channels_gae: 5
  lr: 0.01
  batch_size: 32
  num_workers: 2
  train_share: 0.8
paths:
  graph_adjacencies: ${hydra:runtime.cwd}
  graph_labels: ${hydra:runtime.cwd}
